services:
  ip-checker:
    build: .
    container_name: ip_checker_site
    restart: unless-stopped
    networks:
      hysteria_vpn:
        ipv4_address: 10.60.0.4

networks:
  hysteria_vpn:
    external: true