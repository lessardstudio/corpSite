services:
  ip-checker:
    build: .
    container_name: ip_checker_site
    restart: unless-stopped
    networks:
      - internal
    ports:
      - "10.60.0.4:80:80"

networks:
  internal:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/24